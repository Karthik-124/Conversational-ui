# Legalos Backend

Flask-based HTTP API wrapper for the Legalos RAG system.

---

## Structure

       legalos/
           ├── app.py
           ├── requirements.txt
           ├── README.md
           └── legalos_package/
               

---

## Running Locally
### Prerequisites

Before running **Legalos**, make sure you have:

**Python 3.11**
  > Python 3.11 works best with LangChain. Newer versions may cause compatibility issues.        
 **pip** (or any Python package manager)

> **Running scripts:** This project uses the **`-m` module structure**. Run Python scripts from the **project root** (`legalos/`) with `python -m chatbot.<module>`, e.g.:
>
> ```bash
> python -m chatbot.main --config ./config/rag_v1.json
> ```
###  Install dependencies
```bash
pip install -r requirements.txt
python app.py
```

This Runs on:-
```
http://localhost:5000
```

## API
GET /chat
```
GET /chat?message=<your_query_here>
```

POST /chat
```
POST /chat
Content-Type: application/json

{
  "message": "your query here"
}
```
## Notes

1.Core RAG logic lives in legalos_package/chatbot

2.factsDB and vectorDB are intentionally excluded from version control,they have to be generated by the user.

